apiVersion: v1
kind: Service
metadata:
    name: saikumarbomminenidotnetdemo
spec:
    type: LoadBalancer
    ports:
    - port: 5000 
    selector:
        app: saikumarbomminenidotnetdemo
